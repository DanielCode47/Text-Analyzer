import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'TextLens — AI Text Analyzer',
  description: 'Extract themes, keywords, summary and emotional tone from any text',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
